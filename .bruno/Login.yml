info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_ADDRESS}}/auth/login"
  body:
    type: json
    data: |-
      {
        "Username": "test123",
        "PasswordHash": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
