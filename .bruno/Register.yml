info:
  name: Register
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_ADDRESS}}/auth/register"
  body:
    type: json
    data: |-
      {
        "Email": "test@gmail.com",
        "Username": "test123",
        "Password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
